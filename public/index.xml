<?xml version="1.0" encoding="utf-8" standalone="yes"?>
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:media="http://search.yahoo.com/mrss/">
	<channel>
		<title>Carlos Santos</title>
		<link>http://localhost:1313/</link>
		<description>Recent content on Carlos Santos</description>
		<generator>Hugo -- 0.159.0</generator>
		<language>en</language>
		<lastBuildDate>Tue, 24 Mar 2026 00:00:00 +0000</lastBuildDate>
		<atom:link href="http://localhost:1313/index.xml" rel="self" type="application/rss+xml" />
		
    	<image>
			<title>Carlos Santos</title>
    	  	<url>http://localhost:1313/squelette_et_cigarette.jpeg</url>
    	  	<link>http://localhost:1313/squelette_et_cigarette.jpeg</link>
    	</image>
		
		<item>
			<title>Payjoin POC: Implementing no_std on payjoin ecosystem</title>
			<link>http://localhost:1313/posts/payjoin-poc/</link>
			<pubDate>Tue, 24 Mar 2026 00:00:00 +0000</pubDate><author>Carlos Santos</author><guid>http://localhost:1313/posts/payjoin-poc/</guid>
			<description><![CDATA[This logbook documents my journey through the Vinteum Bitcoin Developer Launchpad PoC.]]></description><content type="text/html" mode="escaped"><![CDATA[<p>When I started this POC through the Vinteum Bitcoin Developer Launchpad, the goal was straightforward: make the <code>payjoin</code> crate compile and run correctly in a <code>no_std</code> environment. What followed was a deep dive into Rust&rsquo;s feature system, embedded targets, and the subtle ways that <code>std</code> leaks into code you think is clean.</p>
<h2 id="the-problem">The problem<a href="#the-problem" class="anchor" aria-hidden="true"><svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"
      stroke-linecap="round" stroke-linejoin="round" class="feather">
      <path d="M15 7h3a5 5 0 0 1 5 5 5 5 0 0 1-5 5h-3m-6 0H6a5 5 0 0 1-5-5 5 5 0 0 1 5-5h3"></path>
      <line x1="8" y1="12" x2="16" y2="12"></line>
   </svg></a></h2>
<p>Most Rust crates are written with <code>std</code> as an implicit assumption. Strings, collections, error traits, I/O — all of it silently pulls in the standard library. For hardware signers and embedded environments, this is a hard blocker. The <code>payjoin</code> crate needed to work without it.</p>
<p>The goal: make <code>cargo build -p payjoin --no-default-features --features &quot;v2,alloc&quot;</code> pass cleanly.</p>
<h2 id="first-attempts">First attempts<a href="#first-attempts" class="anchor" aria-hidden="true"><svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"
      stroke-linecap="round" stroke-linejoin="round" class="feather">
      <path d="M15 7h3a5 5 0 0 1 5 5 5 5 0 0 1-5 5h-3m-6 0H6a5 5 0 0 1-5-5 5 5 0 0 1 5-5h3"></path>
      <line x1="8" y1="12" x2="16" y2="12"></line>
   </svg></a></h2>
<p>The early builds failed in expected ways. The <code>wasm32-unknown-unknown</code> target broke immediately because <code>getrandom</code> doesn&rsquo;t support it without specific flags. Switching to <code>thumbv7em-none-eabihf</code> got further, but hit a wall at <code>secp256k1-sys</code>, which requires <code>arm-none-eabi-gcc</code> to compile its C bindings.</p>
<p>The pattern became clear early: dependency graphs are deep, and <code>std</code> assumptions hide everywhere — in <code>openssl-sys</code>, in <code>serde_json</code>, in anything that touches I/O or time.</p>
<h2 id="isolating-std">Isolating std<a href="#isolating-std" class="anchor" aria-hidden="true"><svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"
      stroke-linecap="round" stroke-linejoin="round" class="feather">
      <path d="M15 7h3a5 5 0 0 1 5 5 5 5 0 0 1-5 5h-3m-6 0H6a5 5 0 0 1-5-5 5 5 0 0 1 5-5h3"></path>
      <line x1="8" y1="12" x2="16" y2="12"></line>
   </svg></a></h2>
<p>The core approach was systematic replacement:</p>
<ul>
<li><code>std::string::String</code> → <code>alloc::string::String</code></li>
<li><code>std::vec::Vec</code> → <code>alloc::vec::Vec</code></li>
<li><code>std::collections::BTreeMap</code> → <code>alloc::collections::BTreeMap</code></li>
<li><code>std::error::Error</code> → <code>core::error::Error</code></li>
</ul>
<p>Any module touching I/O, HTTP, JSON, OHTTP, or system time got gated behind <code>#[cfg(feature = &quot;std&quot;)]</code>. In <code>no_std</code> builds, those paths return a clear implementation error instead of silently failing or pulling in unwanted dependencies.</p>
<h2 id="the-tests-lie">The tests lie<a href="#the-tests-lie" class="anchor" aria-hidden="true"><svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"
      stroke-linecap="round" stroke-linejoin="round" class="feather">
      <path d="M15 7h3a5 5 0 0 1 5 5 5 5 0 0 1-5 5h-3m-6 0H6a5 5 0 0 1-5-5 5 5 0 0 1 5-5h3"></path>
      <line x1="8" y1="12" x2="16" y2="12"></line>
   </svg></a></h2>
<p>One of the most important lessons: <strong>passing tests don&rsquo;t guarantee <code>no_std</code> compatibility.</strong></p>
<p>The test harness implicitly enables <code>std</code>. A build that passes <code>cargo test</code> can still fail <code>cargo build --no-default-features</code>. The only way to verify real <code>no_std</code> compatibility is to run the build directly, without the test harness enabling <code>std</code> behind the scenes.</p>
<p>This led to a validation script that became the standard check throughout the POC:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>clear <span style="color:#f92672">&amp;&amp;</span> <span style="color:#ae81ff">\
</span></span></span><span style="display:flex;"><span>cargo build -p payjoin --no-default-features --features <span style="color:#e6db74">&#34;v2,alloc&#34;</span> <span style="color:#f92672">&amp;&amp;</span> <span style="color:#ae81ff">\
</span></span></span><span style="display:flex;"><span>cargo build -p payjoin --no-default-features --features <span style="color:#e6db74">&#34;std,v2&#34;</span> <span style="color:#f92672">&amp;&amp;</span> <span style="color:#ae81ff">\
</span></span></span><span style="display:flex;"><span>cargo test  -p payjoin --no-default-features --features <span style="color:#e6db74">&#34;alloc,v2&#34;</span> <span style="color:#f92672">&amp;&amp;</span> <span style="color:#ae81ff">\
</span></span></span><span style="display:flex;"><span>cargo test  -p payjoin --no-default-features --features <span style="color:#e6db74">&#34;std,v2&#34;</span>
</span></span></code></pre></div><h2 id="feature-gating-and-the-uri-parser">Feature gating and the URI parser<a href="#feature-gating-and-the-uri-parser" class="anchor" aria-hidden="true"><svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"
      stroke-linecap="round" stroke-linejoin="round" class="feather">
      <path d="M15 7h3a5 5 0 0 1 5 5 5 5 0 0 1-5 5h-3m-6 0H6a5 5 0 0 1-5-5 5 5 0 0 1 5-5h3"></path>
      <line x1="8" y1="12" x2="16" y2="12"></line>
   </svg></a></h2>
<p>The V1/V2 URI parser required careful feature gating. V1 (BIP 78) uses only <code>OH1</code> in the fragment. V2 (BIP 77) requires <code>RK1+OH1+EX1</code>. When running with only <code>--features &quot;v2&quot;</code>, the entire V1 code path is removed from compilation — tests that referenced <code>PjParam::V1</code> simply didn&rsquo;t compile.</p>
<p>The fix was adding <code>#[cfg(feature = &quot;v1&quot;)]</code> to 9 tests and <code>#[cfg(all(feature = &quot;v1&quot;, feature = &quot;v2&quot;))]</code> to tests covering the V2→V1 fallback path. After gating, 142 tests ran clean on V2-only builds and 163 on V1+V2.</p>
<h2 id="architectural-decision">Architectural decision<a href="#architectural-decision" class="anchor" aria-hidden="true"><svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"
      stroke-linecap="round" stroke-linejoin="round" class="feather">
      <path d="M15 7h3a5 5 0 0 1 5 5 5 5 0 0 1-5 5h-3m-6 0H6a5 5 0 0 1-5-5 5 5 0 0 1 5-5h3"></path>
      <line x1="8" y1="12" x2="16" y2="12"></line>
   </svg></a></h2>
<p>Functions that depend on I/O, HTTP parsing, JSON, or system time are <code>std</code>-only. There&rsquo;s no stub, no workaround. In <code>no_std</code> builds, those paths fail explicitly. This keeps the core protocol — state machine, fallback transaction, PSBT handling, URI parsing — genuinely portable without hidden compromises.</p>
<h2 id="what-works-in-no_std">What works in no_std<a href="#what-works-in-no_std" class="anchor" aria-hidden="true"><svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"
      stroke-linecap="round" stroke-linejoin="round" class="feather">
      <path d="M15 7h3a5 5 0 0 1 5 5 5 5 0 0 1-5 5h-3m-6 0H6a5 5 0 0 1-5-5 5 5 0 0 1 5-5h3"></path>
      <line x1="8" y1="12" x2="16" y2="12"></line>
   </svg></a></h2>
<p>By the end of the POC, the following were fully functional under <code>no_std + alloc</code>:</p>
<ul>
<li>State machine for V2 sessions</li>
<li>Fallback transaction parsing and processing</li>
<li>URI parsing with V2 parameters</li>
<li>Core collections and type handling via <code>alloc</code></li>
</ul>
<h2 id="commit-history">Commit history<a href="#commit-history" class="anchor" aria-hidden="true"><svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"
      stroke-linecap="round" stroke-linejoin="round" class="feather">
      <path d="M15 7h3a5 5 0 0 1 5 5 5 5 0 0 1-5 5h-3m-6 0H6a5 5 0 0 1-5-5 5 5 0 0 1 5-5h3"></path>
      <line x1="8" y1="12" x2="16" y2="12"></line>
   </svg></a></h2>
<p>The final step was a full interactive rebase to clean the commit tree. Two months of &ldquo;broke and fixed&rdquo; commits were squashed into a coherent narrative showing the real progression of <code>no_std</code> support. The branch <code>poc/no-std-payjoin</code> reflects the work as it should be read, not as it was written.</p>
<h2 id="takeaways">Takeaways<a href="#takeaways" class="anchor" aria-hidden="true"><svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"
      stroke-linecap="round" stroke-linejoin="round" class="feather">
      <path d="M15 7h3a5 5 0 0 1 5 5 5 5 0 0 1-5 5h-3m-6 0H6a5 5 0 0 1-5-5 5 5 0 0 1 5-5h3"></path>
      <line x1="8" y1="12" x2="16" y2="12"></line>
   </svg></a></h2>
<ul>
<li><code>no_std</code> compatibility requires explicit architecture decisions, not just <code>#[cfg]</code> patches</li>
<li>Feature flags in Cargo remove code completely — design around that, don&rsquo;t fight it</li>
<li>The test harness enables <code>std</code>; always verify with a direct build</li>
<li><code>core::error::Error</code> is available in recent Rust without <code>std</code></li>
<li>Dependency graphs carry <code>std</code> assumptions deep — audit early</li>
</ul>
]]></content>
		</item>
		
	</channel>
</rss>
